# 教训与根因（每条对应一次真实返工；新片结束后把新教训追加到这里）

## 构建产物（最佳实践）
- `public/` 只放本片资产（模板 18MB，bundle ≈38MB）；出 still 只用 `scripts/still.sh`，每组一个固定 tag，改码后 `rm -rf build_dev_<tag>` 再建；不要直接 `npx remotion still src/index.ts …`（会在 $TMPDIR 留临时 bundle，脚本已自动清理）。
- 后台脚本一律绝对路径（shell cwd 会漂移）。

## 分镜写法
- "末 2 帧不离场 + 下一镜头硬切"会被两侧理解成"我不动、你从节拍帧才入场"→ 组界/镜头间 6–8 帧空场（一片里 5 处）。写成"A 末 N 帧归零淡出 + B 首帧起入场"。
- 硬切前离场必须归零：6.7%/帧的 exitFade 末帧还剩 40–57%，动态下会"啪"。用 1−(n/N)^1.5。
- 自下滑入 Δ≈300 在有字幕带的片里会穿过 y637–690（一片里 6 处）。Δ≤120 或侧向滑入 + 前 6 帧渐入。
- 多元素错峰入场别用 GlitchIn（5 个胶囊各自随机闪像掉帧）；用错峰 slide+fade。
- "glitch"一词只写在每镜头一个重点词那一格，否则构建 agent 会给所有文字加闪烁（一片 102 处，用户否决）。
- 分镜给的坐标是参考值，允许构建组按安全区微调；但示例文本、颜色语义、图元必须照办。

## 覆盖层 / 结构
- 覆盖层的压黑/淡出必须在内容之上（SHOTS 数组末尾或 aboveBar），否则末镜头在自己 Sequence 结束时硬切消失。
- 出场动画帧数要够：8 帧 exitAccel(1.2) 只走了 59px/63% 亮度就被硬切；12 帧 440px + 透明度归零才干净。
- HUD 同章相邻条目要无空档（上一条延到下一条 from−1）；进章节卡前 8 帧淡出；HUD 换词与内容大 glitch 错开 4 帧，否则同帧全黑。
- 流程轨末 8 帧淡出，不要一帧消失。
- 预览合成（每组 = 覆盖层 + 本组镜头）让构建组能看到 HUD/流程轨与内容的关系。

## 代码
- kf 首值陷阱：t < 首关键帧返回首值；入场表要以 [镜头起始帧, 画外值] 开头，否则元素提前停在画内。
- Check/Cross 等 draw-on 在 p=0 时要 return null（linecap 会露出一个点）。
- 颜色混合函数要能解析 rgb() 与 hex（嵌套解析出 NaN 导致 25 张卡全不可见）。
- blur σ<0.8 在 Chromium 无效；feConvolveMatrix 走软件路径 0.13 fps 禁用；SVG filter 加 colorInterpolationFilters="sRGB"。
- CJK 行盒 ascent 让墨迹比 top 低 3–7px，居中文字预扣 dy −2…−5。
- 最小字号 22px（QC 量测 ink 高）；上标角标可例外 20px。

## 配音 / 时间轴
- 定稿后不要改词：构建组代码里帧号硬编码。要改先改词再全体重对位。
- TTS 词边界起点与音频 onset 误差 ≈1 帧；检测 onset 时窗口别跨到上一句尾音（会误读 −15 帧）。
- 英文 `|` 切块拼回整句时块之间要补空格（曾把 `powerful|but` 拼成 powerfulbut 送给 edge-tts）；块起始游标也要跳过这个空格。

## 多 agent
- 一个 agent 只派一件重活（构建 5–7 镜头 / QC 一章 / 修 1–2 组）；第二件只派轻活。读过大量图片的 agent 再派重活有 ~30% "input too long" 阵亡。
- API 错误打死的 agent：SendMessage 叫一次可醒；不醒就 TaskStop 重派。
- 构建组会自行合理偏离（换示例文本、加中文全称、改拓扑、缩帧数）；有出处就放行，一句话裁定；把裁定记进 BUILD_NOTES。
- QC 读亮度用 int32（int16 溢出全零）。

---
# 第二片《eCPM 与广告竞价》（2026-09-07，48 镜头 / 4′16″）新增教训

## 节拍与可读时间（本片最系统性的两类缺陷）
- **出处/依据灰小字不要挂在镜头的最后一个字幕块上。** 分镜表默认把出处写在末拍，结果 40–70 字的灰小字 ≥50% α 只停 0.97–1.7 s，读不完 —— 其中一条是"防止观众把公式误认成现行定义"的纠偏句，等于没送达（QC 判高）。**判据：出处小字必须在所属镜头内有 ≥2.5 s（≥75 帧）的 ≥50% α 停留；写法：f0 挂到镜头的第 1–2 个字幕块**（它是背景依据，不需要跟着解说走）。本片 8 处全部返工。
- **镜头区间 `[句 from−8, 句 to+2]` 开头那 8 帧就是入场预算，别浪费。** 首批元素若等到字幕块起始帧才动，镜头首段会空屏；组界处（前一镜头末 8 帧已归零）会叠成连续 11–16 帧纯黑。本片 9 个镜头中招。**首批元素的 f0 = 镜头首帧。**
- **末拍动画会溢出镜头末尾。** 最后一个字幕块到镜头结束可能只有 20 帧，22 帧的 slideUp、14 帧的 Cross draw-on 都放不进去 —— 成片上表现为"某个元素整个镜头都没出现，到下一镜头首帧突然满态"。派单时给每个镜头算一下末拍剩余帧数。

## `fadeIn` 与首帧
- **`fadeIn(n, len)` 在 n=0 返回 0，用它做镜头首帧入场必然空一帧。** 用 `softOp`（首帧 ≈25%）。
- 更隐蔽的一层：**`softOp(0, 8)` = 25%，白描边亮度只有 65，仍低于"亮度 >70"的空场判据** —— 想让首帧测得出来要写 `softOp(n+1, 6)`（首帧 ≈57%）。同理，紫边 `#A175F1`（亮度 144）在 43% α 下只有 62，也量不到；入场首帧想被量到就得用白边或提高起始 α。
- **不要给自带 opacity 斜坡的组件再套一层 `fadeIn`**（双重淡入）：转场处会凹一两帧。
- **同一元素不要渲染两份**（一份 `SoftIn` 常亮 + 一份 `GlitchIn`）：常亮那份把闪烁完全遮住，实测亮度恒定、闪烁等于没做。要么单实例用 `glitchOpacity(n)` 乘在同一个元素上，要么让常亮那份在闪烁窗口内隐藏。

## 数字的事实合规
- **计数动画的中间值也是"上画面的数字"。** 年份 1995→1998 线性计数会让 1995/1996/1997 清晰可读 4–6 帧；毫秒 0→120 会闪出 `37 ms`/`88 ms`；eCPM 0→30 会闪出 `8`。这些都不在事实清单内。**通则：中间值若是"看起来像事实但没出处"的数字，要么让它不可读，要么不做计数。**
- 让年份不可读要够狠：blur `6.3→1.3px` 中间年份清晰可读；`12→2px` 末段 ≤7px 时倒数第二个年份仍可辨约 2 帧；**最终 `12→9px`（滚动全程 ≥9px）+ 落定帧归 0** 才干净。已封成模板的 `YearRoll`。
- **自建示意算例必须与画面上的其它标注自洽。** 本片两处翻车：① 一个「本秒 ＋2405」的实时计数器，正下方紧跟灰小字「一天几十亿次」—— 两者相差 30 倍；② 价值柱公式算出第 8 根 ≈34，而画面自己标着「≈ 1/5」（应为 24）。**派单前把分镜表里每个自建算例横向验算一遍。**
- 补一条查法：**从源码静态抽取所有会上画面的字面量，和事实清单对账。** QC 是从像素抽帧查的，看不到没被抽到的帧；源码对账把 8 个组一次扫完，本片就是这么抓到那个计数器的（四个 QC 全漏）。

## 字体
- **`Orbitron[wght].ttf` 不含 `¥`(U+00A5)、`≠`(U+2260)、`≈`(U+2248)**（fontTools 可核）。回退链 `'Orbitron','Audiowide'` 要保留 —— Audiowide 同为几何风格，¥ 与 Orbitron 数字是协调的（放大 3 倍实测；换成回退 Noto 反而更不搭）。`≠`/`≈` 直接用 `FONT_HEAVY`。
- **Orbitron 的斜杠零（Ø）在多位数里没问题，孤立成单个 `0` 时会被读成图标**（「0 次点击」成片上读作「Ø 次点击」，示意值直接失效）。**孤立单字 `0` 改 `FONT_HEAVY`；`1`–`9` 字形无歧义可保留。**
- **`Counter` 的默认字族是 `FONT_HEAVY` 不是 Orbitron**，与风格指南「数字用 Orbitron」冲突 —— 本片四个组同时反馈并各自绕开自建了一份。已把默认改成 `FONT_ORB` 并补 `family` 入口。

## 图元与层序
- **旋转类图元的几何要先验算再画。** `GavelIcon` 旧版把槌头画在支点左上方 76 单位、音块在支点下方 101 单位 —— **槌头到支点的距离远大于支点到接触目标的距离，于是 ±26° 的小角度旋转无论正负都只让槌头绕着高处扫，永远碰不到音块**（QC 实测槌头质心 y 507→499 是往上走的）。这不是参数问题。正确造型：接触物水平躺在目标上方、握柄竖直、支点在握柄顶端。
- **落槌类动作不要停在"接触姿态"。** 重写后 `angle=0` 是"竖直细握柄 + 槌头压在音块上"，长时间停在这个姿态被读成橡皮图章/通马桶（两个组独立发现）。落槌后要抬回 −18…−24°。
- **draw-on 的叉/勾不要和箭头挤在同一个 `<Svg>` 里。** 若那个 `<Svg>` 排在卡片组件之前，叉会被卡片的黑底整块盖掉 —— 表现为"这个叉全片不出现"，很容易误判成时序问题。**凡是要压在卡片上的标记，`<Svg>` 必须排在卡片之后。**
- 图元把 `title` 画在**卡外上方**时要在注释里写明，否则有流程轨的章会顶到 rail；`xLabel` 画在 `cy = h−8` 会让 18px CJK 墨迹压死卡下边框（本片三张卡全中）。

## 覆盖层
- **进度条的当前章高亮要切在章节卡起始帧，不是本章首句起始帧。** 否则章节卡在画面正中宣告「02 一次竞价」的那 45 帧里，底部进度条还高亮着上一章，两处信息矛盾。
- **流程轨当前步的切换不要做成 1 帧硬切**（灰底↔紫底同帧翻转），与 HUD 换词「一律 8 帧淡入」不一致。加 7 帧交叉过渡。
- 流程轨入场的上浮 Δ 要 ≤22：Δ40 会让胶囊下沉到 y184，越过"有轨镜头内容不进 y<175"的空间契约。
- 片尾 `EndingTop` 的淡入帧数决定"末段纯黑"有多长：20 帧只剩末 11 帧纯黑，改 10 帧才有末 19–21 帧。
- **片尾压黑的起始帧会吃掉末镜头的收尾动作。** `endingFade=30` 时压黑从"末句结束 −30"开始，本片的收尾敲槌原本落在其后，全片最后一个动作完全看不见。派单时把末镜头的动作排进压黑之前，或调小 `endingFade`。

## 多 agent
- **tmux/fork 有上限，且是全机共享的。** 本机同时开着 13 个其他 Claude 会话，派到第 8 个构建组时 `respawn pane failed: fork failed: Device not configured`，两次重试都起不来 —— 主会话只能自己顶下一整组（6 个镜头）。**派单前先 `ListAgents` 看全机占用；预留 1–2 个 pane 给后续的 QC/修复轮；完成的 agent 及时 TaskStop 释放。**
- **"边查边 append"对 QC agent 是硬要求，不是建议。** 本片一个 QC agent 攒到最后写盘，跑了一小时零产出、消息也不回，只能 TaskStop 重派（重派时把主会话已查到的结论直接给它，并让它 QC+修复一并做，反而更快）。
- **不要照抄 agent 的自评。** 本片抽验发现：一个组自评"中间年份模糊不可读"，实际出图清晰可读（它自己在复盘时也承认"§6 偏离①的自评过于乐观"）；另一个组报告"已改为调用共用 exitOp"而磁盘时间戳看起来更早（这条查下来是我误读，报告是对的）。**结论：报告要抽验，但也别急着断定 agent 说错了 —— 先把证据看全。**
- **构建组会发现分镜表本身的错误，要鼓励并快速裁定。** 本片三条最有价值的改进都来自构建组：价值柱公式与画面标注矛盾、年份转盘的事实合规漏洞、以及据此主动偏离不给毫秒数做计数。裁定时把它推广成全片通则并写回分镜表，比只批准单点改动值钱得多。
- 主会话自己要留一手**与 QC 互补的检查**：帧覆盖静态分析（帧区间/空洞/重叠）、源码字面量与事实清单对账、闪烁白名单源码级计数。这三项都不用渲染，几秒钟跑完，而且抓到的都是 QC 从像素查不到的问题。

## 风格传递（2026-09-08 复盘：用 skill 做的《广告竞价》vs 样片《RAG》）
- **skill 写了禁令没写审美驱动**：安全区、白名单、归零、节拍全传过去了（新片在这些纪律项上甚至更好），但「画多大、谁发光、高光时刻怎么编排」只在做样片的人脑子里 → 新片主体中位高度 172px vs 219px、29% 时长最大物体 <110px、主角全是无光白线框、高光时刻用「胶囊 + 图标 + 一行字」应付。修法：`composition-and-light.md` 硬规则 + 分镜表加「主角·尺寸」「光」两列 + 高光时刻清单。
- **样片源码没人读**：8 个构建组 BUILD_NOTES 零处提到 shots_src，光效图元（LightBar/HaloRing/GhostText）留在样片组内文件里没进模板 → 新片一个组重新发明了 LightBar。修法：光效图元升进 `template/src/fx.tsx`；模式表最后一列给源码路径，构建 prompt 从「可参考」改成「必读」。
- **QC 只查缺陷不查「空 / 小 / 暗」**：四轮 QC 全过，画面还是小而暗。修法：`scripts/frame_metrics.py` 量最大物体高度 / 主角区柔光 / 碎片，写进 QC 判据与终检。
- **运镜被本文自己压掉了**：旧版 build-rules 写「静态镜头为主」，结果样片和新片都只有 2.2–2.5 次/分钟、零次推近，而风格来源的原片是 6.6 次/分钟、平均 16 帧、含推近拉远视差。修法：`CameraRig` 组件 + 运镜预算（每章 ≥3）+ 分镜表运镜清单。
- 两片动效数量、节奏、柔光总面积几乎相同（每帧变化 0.47 vs 0.56，事件 72 vs 76 /分钟）——**差距不在「动得多不多」**，别往加动效的方向修。


---
# 第三片《电影运镜》（2026-09-08，50 镜头 / 4′00″，一轮 QC 后 高0/中0/低14）新增教训

## 当主题本身是几何的：先造一个真的，别用 CSS 假造
- 本片的中心论点是「透视只由机位决定，与焦距无关」。**如果画面用 `scale` 假造，推近和变焦会长得一模一样，整章等于讲错。** 所以在 `ui.tsx` 里实现了真的小孔成像投影（`project3` / `FrameScene` / `camOrbit` / `camDollyZoom`），推 = 只改 `cam.z`、变焦 = 只改 `cam.f`、摇 = 只改 `cam.yaw`…… QC 逐像素验证：SC37 两框人物 275/275px 相等而背景门高 197 vs 158px（差 20%），SC38 人物全程恒 164px 而后墙 506→360px。**论点因此是"画面证明"的，不是"旁白声称"的。**
- 通则：**凡是"两种做法看起来像但其实不同"的主题（推 vs 变焦、tilt vs 升降、真长镜头 vs 假长镜头），都必须让底层机制真的不同**，否则观众看到的两幅图会一样。
- 反面成本：真投影会带来一整类新缺陷（画框变空、物体越出画框、人物尺寸出安全带），见下条。

## 派 8 个 agent 之前，先用探针把共用系统跑一遍
- 我先写了一个 `_probe.tsx`，一帧一格渲染 8 种相机状态，出 8 张图看一眼。**当场抓到两个会让 8 个组集体做错的问题**：① `pitch=−22°` 时画框几乎全空（人物 180px 但画框 332px，人整体在框内，pitch 一动就把人扫出去只剩地板）；② 默认 `personZ=520` 时人与后墙只差 165cm，推 vs 变焦的背景差别只有 12%，**肉眼看不出来**——必须把人挪近到 `personZ=320` 才有 26% 的差别。
- 这两条如果不提前发现，就是 SC17 与 SC37/SC38 三个镜头（含两个高光时刻）集体报废。**探针成本 10 分钟，返工成本 3 个组。**
- 探针的产物要变成分镜表里的**「相机参数速查」实测表**（`cam.z` → 人物画面高的对照表 + 安全带 150–300px），派单时直接给数字，不要让每个组自己踩。

## 跨组一致性：把常量放进共用层，不要让 agent 互相对齐
- v1 里我一开始让「G6 建矩阵、G7 复用」「G5 复用 G3 的间距标注」——结果 G7 要跨组 import、G6 与 G3 各存了一份逐参数复制的 `PinCross`，并且 G3 一改 G5 就跟着变。
- 更好的做法（本片后半采用）：主会话**先把跨组共用的东西放进 `ui.tsx`**——`LAYOUT`（版式坐标）、`frameInner`、`frameMarks`（门/人/信的包围盒，用同一投影算）、`ACTION_WORDS`、`softOp` / `exitZero` / `mix` / `Vignette` / `MarkBox`。八个组直接 import，天然一致，零协调成本。
- 教训的具体形态：**`softOp` 被四个组各写了一份，四份公式还不一样**（G1/G2 的在 n=0 返回 0，靠调用处传 `n+1` 补偿；G3/G4 的把 +1 写进函数）。协议里反复写「用 softOp 不要用 fadeIn」却没在共用层提供它，等于逼每个组自己发明。**协议提到的工具必须在共用层真的存在。**

## 高光时刻的"清场 → 扫光"开场本身会被判空场
- v1 两条"高"都是这个形态：SC12（论点型）前 79 帧只有一条舞台光线 + 一轮扫光细带，最大物体 ≤46px；SC40 前 66 帧只有五个未激活的暗框 + 一枚胶囊。
- `composition-and-light.md` §6 写了「扫光 / 舞台光线阶段（内容区柔光 ≥10000px²）不算空场」——但**一轮扫光的柔光面积够不上 10000px²**（实测 5004px²）。于是"按编排做的清场开场"反而触发空场判据。
- 修法有二，最好同时做：① **扫光加到三轮**并把 `StageLine` 柔光半径加倍，真正落进豁免区；② **在第 1 个节拍就给一个临时主角**（本片把上一镜的大字承接过来缩到 120px，或把该拍的关键词直接做成 96px 大字，下一拍再缩小让位）。
- 通则：**登场型/论点型高光时刻的前 60–80 帧要单独算一遍"这几帧最大物体多大"**，别假设"清场 + 光效"能自动过关。

## 片尾压黑 vs 最后一个字幕块：要算，而且要算两次
- lessons 里已经有「片尾压黑会吃掉末镜头收尾动作」，本片踩的是它的**升级版**：不是动作被吃掉，而是**全片结论大字只有 6 帧（0.20 s）满态可读**。
- 成因是三件事叠加：末字幕块起于 f7067 → `GlitchIn` 12 帧（**其中 7 帧字是灭的**）→ 压黑 f7085 起吃亮度。18 帧的窗口里能读的只有 6 帧。
- **判据**：`(S_last.to − endingFade) − 末字幕块起始帧 ≥ 30 帧`，否则结论读不完。`GlitchIn` 的 12 帧里有一半是"灭"的，等于**实际可读时间只有窗口的一半**——用 glitch 做收尾大字时要按双倍预算算。
- 本片的修法：把**整句**结论提前到倒数第二拍（f7043）一次入场，末拍改由"光在动"（光环波纹加密 + 柔光脉冲 + 描边渐显）承担可察觉变化。**注意提前的落点不能压在白闪帧上**——QC 发现我原本提议的 f7040 正好是一段运镜白闪的首帧，glitch 落上去会读成"闪坏了"，改 f7043。
- 派单时就该给末镜头算这张表：末字幕块起始帧 / 压黑起始帧 / 中间有没有白闪 / glitch 的有效可读帧数。

## 变换缩放会把字号带下去
- SC34/SC39 的十个动作胶囊用 `scale(0.72)` 做承接位移，26px 的字变成 **18.7px**，跌破「最小字号 22px」。QC 从像素查不出来（它量的是 ink 高度，18.7px 的 CJK ink 约 14px，容易被当成"小标签"放过），是构建组 G7 自己算出来报的。
- **通则：凡是整组 `scale`/`transform` 的地方，把里面所有字号乘一遍缩放系数再对照 22px 下限。** 修法是改源头字号（26→31，31×0.72 = 22.3），不是改缩放。

## 双联版式：左右两半必须真的是同一台相机
- 本片主版式是「左俯视平面图 + 右取景框」。v1 里平面图把门画在**左墙**，而画框里门在**后墙**——两半互相矛盾，而且平面图的视锥里根本没有门。QC 一眼看出来。
- 成因：`roomAnchors()` 是我按"平面图好看"手写的示意坐标，`SCENE` 是按世界几何写的，两者从没对齐过。**修法：让平面图的门位由 `SCENE` 反算**（世界 x=−245 在 x∈[−440,440] 里的归一化位置 0.2216），门画到上墙。
- 但也要认清代价：**把平面图改成 `SCENE` 的严格投影会移动所有组已调好的视锥角度**。本片只做了低风险的一半（门位归正 + 信封缩小），剩下的（相机朝向、视锥张角、房间比例）作为"示意图不是严格投影"记进交付说明。**这类改动要么在派单前做完，要么就承认它是示意图。**
- 同一个图元在不同组里颜色不一致也属这类：`FovCone` 默认色是紫，G5 显式传了灰、G6 用了默认 → 第 3 章上下半视锥颜色不同，还是 SC35「紫色碎片≥8」的主因。**默认值要按规范设（配角不发光 → 默认中性灰），不要让"传不传参数"决定风格。**

## 房间/场景几何要先想清楚"镜头会转到哪"
- `SCENE` 一开始只有后墙、且只到 x=±440，没有侧墙。结果**大角度摇（|yaw|≳8°）、环绕、横移时画框前导侧露出成片纯黑**——G3 被迫把甩镜落点从 +42° 收到 +22°，G4 被迫把 SC19 两框的 yaw 差从 60° 收到 26°，而 G6 的 SC32 要环绕 55°（θ=55° 时后墙右缘只到画框 61%，39% 是空区）。
- 补两面侧墙（逐段画 + 逐段判可见性，推近到侧墙之后能优雅退化）一处改动同时救了三个组，**而且侧墙的透视收敛线反而成了「横移有视差」「背景独自被抽走」最好的证据**——G5、G6 都主动回报效果变好。
- 通则：**场景几何要按"最极端的那次运镜"来设计**，不是按默认机位好看来设计。派单前把所有运镜的角度/位移列一遍，问一句"这个角度下画框四边各是什么"。

## 多 agent（本片新增）
- **pane 上限又撞了，且比上次早**：本机同时开着 14 个其他会话，我派到**第 5 个** agent 就 `respawn pane failed: fork failed: Device not configured`（上一片是第 8 个）。**修法：按 4 个一波派，一波完成并 `TaskStop` 释放后再派下一波**；派单前 `ListAgents` 看全机占用。
- **QC agent 被 API 错误打死了两个**（"The response stopped arriving"、ENOTFOUND）。`SendMessage` 叫一次都醒了，接着做完。**但醒过来只保住了已写盘的部分**——所以"边查边 append"不是建议而是硬要求：本片一个 QC agent 跑了 46 分钟仍是 0 字节，我催了一次它才开始写，如果它先死掉就全丢了。
- **QC 的量化验证比"看图说 OK"值钱得多。** 本片最有价值的 QC 产出是两组逐像素读数（SC37 的 275/275px 与 197/158px、SC38 的恒 164px 与 0.711×）——它们把"论点成立"从主观判断变成了可复核的证据，直接写进了交付说明。派 QC 时要**指名要哪几个读数**。
- **QC 会修正主会话的修法**。SC50 我提的修法方向对，但 QC 实测发现落点 f7040 压在白闪帧上、且 `CamIcon` 的入场轨迹横穿结论字线——它给的三行修正版比我的原案完整。**派修复单前先让 QC 核一遍自己的修法。**
- **主会话自己写的静态检查（`scripts/selfcheck.py`）值得每片都留**：帧覆盖（50 镜头连续无空洞、与分镜表帧区间对账）、闪烁白名单源码级计数、画面字面量与事实清单对账。几秒跑完，抓到的是 QC 从像素查不到的（例如 `Danny`/`Jack`/`MOVE`/`Dolly` 这些没进事实清单的词）。**噪声过滤要写好**（滤掉 import 路径、CSS 值、camelCase 标识符），否则报一堆假阳性没人看。


## 一分钟《Agentic RL》（2026-09-09）新增教训
- edge-tts 新版 Communicate 默认可能是 SentenceBoundary，而旧 tts_build.py 只收 WordBoundary，会静默留下空列表并退化成按字数估字幕。显式传 `boundary="WordBoundary"`；缓存命中时检查词边界非空，没有词边界应停止而非误报为精准同步。
- 短片缩短 CHAPTER_GAP 后，HUD 原有“间隔<30帧即视为同章”的规则会把上一章胶囊覆盖到下一章章节卡。应按句子的 chapter 比较，而不是猜测间隔。进度条高亮应与章节卡开始对应。
- 只有40帧的片头应重新编排动效预算，不能把长片头默认的 glitch、字幕副标、推镜挤在一起。本片最终选择三行统一6帧淡入，再进行33帧缓动轻推镜；无需强求片头闪烁。
- `mktemp` 生成带点后缀的测渲目录在 Remotion 4.0.507 中可能被当成输出扩展名拒绝。`explainer_test_TAG_XXXXXX` 这样的无点目录可正常输出序列帧。
- 图形光环与标签应看放大帧：仅contact sheet会漏掉白色环线贴住白字的细节。本片对有光环的芯片标签额外下移25px，解决环底穿字。


---
# 第四片《The AI Systems Performance Engineer》（2026-09-10，英文，48 镜头 / 5′07″，9 组，一轮 QC 高 0）新增教训

## 英文片的语速与读音
- **kokoro `am_liam` speed 1.0 实测 ≈2.3 词/秒（散文 2.25、缩写密集句 1.4–1.8），不是 edge-tts 的 2.96**。5 分钟英文片 ≈640 词 / 48 句，按 SKILL.md 表里 700+ 词写会超到 6′17″。第一次跑先合成全稿看 `speech=` 读数再回调篇幅（本片就是这么把 733 词压到 642）。
- kokoro 会把 CUDA / NIXL / MIG / DeepGEMM 逐字母拼读、把 v5.0 读成 "v five zero"。`[词](/音标/)` 覆写语法有效，已做成 `tts_build.py` 的 `PRONOUNCE` 表（只改送 TTS 的文本，字幕不变）。派研究员时顺手让它列出全部缩写，合成前先 dump 音素（`KPipeline` 返回的 `ps`）检查。
- **Noto Sans SC 的弯引号 “ ” 与 … 是 1em 全宽字形**，英文里留出一个大空档；画面上一律用直引号（G9 发现）。
- `Pill` 的 `textDy` 默认 −2 是中文基线补偿，英文片要跟 `TEXT_DY`；模板已改。

## 解说词：按参考资料的目录逐节复述 = 工具清单
- 第一版 48 句按参考书的小节顺序写，用户第一反应是「太死板无趣」；按写作原则独立重写的版本盲评 8.2 对 4.3。根因是没有主线——章可以随意调换顺序而不损失。修法：原则单独成文 `narration-guidance.md`，进阶段 2 必读；`narration-storyboard.md` 不再规定固定章法，只保留格式、预算与画面接口，镜头模式表左列改成题材无关的「画面关系类型」。

## 「入场即停」是缺陷：动词驱动的持续动作
- 用户看 30 秒样片后的第一反应不是风格而是"能不能用更丰富的动效描述这段话"。量化后：4/6 镜头静止帧 42–72%、最长静止 2.8 s；SC04 高光时刻全程在动就明显好看。**样片《RAG》也有同样的毛病，所以"对标样片"筛不出它。**
- 规则（用户裁定，进分镜表全局约束 §9）：每个字幕块的动词有持续到下一拍的动作；入场后不许完全静止 >30 帧；没有其它运镜的镜头加 1.0→1.05 慢推。边界不变（一个主角、一处闪烁、不加碎屑）。量化：内容区 320×180 采样逐帧灰度平均变化 <0.35 为静止帧，静止 ≤40%、最长 ≤1 s（`scripts/motion_check.py`，已进模板）。
- **组级检测（低分辩率 `--scale` 渲染）偏松，成片 fin_frames 复测才是判据**：v1 组级 48/48 达标，成片复测 11 个镜头超限（3 个真静、8 个只有小面积动作——数据包、游标）。已把组级渲染改成 0.5 倍 + JPEG q80，并让 `--frames` 模式多给一列「最长静止段内全分辩率变化像素数」区分真静（<800）与小面积动作。小面积动作的修法是**加大幅度**（运动元素更亮更大更密、主角本体 30 帧呼吸 ±2%），不是加元素。
- 慢推 1.05 对 320×180 采样几乎无贡献，它是给观众的"常动层"，不是给 metric 的。

## 共用层：派单前把跨组要用的东西做进 ui.tsx
- 本片派 G1 前先探针渲了 8 个主题图元（GPUChip / TimelineBar / Gauge / ClusterGrid / RackIcon / PersonIcon / CodeCard / RepoCard），一帧看完再派——零返工。贯穿示例的 `ProfilerPanel`（+ 行数据常量）在 G3/G4 派单前放进 ui.tsx，两组直接 import；后来又补了共用**游标**与 `glowIdleK`，让"同款"有保证。
- **协议提到的工具必须在共用层真的存在**（第三片教训重演）：G1 又自建了 softOp / exitOp / glowOffK / mixHex / GlowBlob / Vignette，主会话在派 G2 前升进共用层并写进 dispatch_notes §0；之后各组零重复。但 camZoomAbout / ArrowPulse / LLMIconLive / FilmPanel 仍被 3–4 个组各写一份——**下一片开工前把它们进模板**。
- `mixHex` 返回 `rgb()`，再喂回 `mixHex` 得 `rgb(NaN)` 且无报错（G8 卡边与分叉描边消失、G9 五枚胶囊边框不渲染）。要么接受 rgb() 输入，要么提供返回 #hex 的版本。
- `TimelineBar` 的 idle 橙光 box-shadow 画在 `overflow:hidden` 容器里会被裁掉（G4 发现）；带光的子元素要画在裁切容器外。`TiltPlane` 的 children 会被 scaleY .5 压扁，文字/图标要"直立"放在层面上（G5）。
- `Vignette` 顶带从 y0 起会压暗 HUD 胶囊（三个组各自发现）；底带到 720 会透过半透明进度条把条压暗。模板已改成 y100–200 / 560–687。
- `CodeCard` tag 16px 低于 22px 下限，四个组都用了它才被 QC 抓到——**共用图元里的字号也要过 22px 下限**。

## 运镜与安全区
- 慢推 1.05 绕 (640,360) 后内容必须落在 **x 89–1191 / y 122–607**，推近 1.25–1.3 按比例算；左右占满的版式（profiler 面板 x190–1090）宁可不慢推。承接位移里胶囊不能随组 scale（字号跌破 22 或胶囊变超宽），几何独立插值。
- **运镜应在节拍前结束而不是从节拍开始**（G2 SC09 把整组平移从 1616–1642 提前到 1590–1616，标签才落在节拍 ±0）。"运镜期间不入新元素"只约束运镜层之外的新元素：随刚体一起平移的胶囊可以在平移中 SoftIn（G4 SC20 QC 中项就是没这么做）。
- 承接两镜头的慢推要连续（SC12 1.0→1.02、SC13 1.02→1.05、SC14 从 1.05 起推近），否则交界帧跳。

## 覆盖层
- **进度条当前章高亮要切在章节卡起始帧**——第二片写过这条教训，模板里却还是切在首句帧（C1/C2/C3/C4 四个 QC 都报了）。这次真的改进了 `ProgressBar.tsx`（按上一章末句 to+3）。
- 片头 tagline 42 字符在 77 帧片头里只满态 13 帧读不完：提前到 a+28，并加了**片尾署名卡 `EndCredit`**（内容压黑 + 末句字幕结束之后才出，aboveBar 层，满态 ≈56 帧）。片尾进度条不能在画面全黑后孤悬 2 s：`EndingTop` 改成随 `endingFade` 同步压黑。
- 署名卡起点必须 > 末句字幕的 `to`（第一版 9090 起时与末字幕、末镜头残影重叠 20 帧），黑尾从 40 帧让到 26 帧换可读时间。

## 多 agent（本片）
- 13 个其它会话开着，按 4 个一波派 9 个组 + 4 个 QC + 4 个修复全程没撞 pane 上限；每个 agent 完成即 `TaskStop`。
- 一个构建 agent 被 ENOTFOUND 打死在 SC02 后，磁盘上其实已写到 SC06——**先看磁盘再决定重派还是唤醒**，一条 SendMessage 就醒了。
- 构建组最有价值的回报仍是"分镜表本身的错"：坐标越界 / 承接与归零矛盾 / 胶囊装不下文字 / 出处小字字号 20 / 节拍与运镜冲突……本片 9 组共报 40 余条，几乎全部批准。分镜表写坐标时**按字宽估算一次**（`textW`）能省一半。
- `test_render.sh` 的 mktemp 模板带点（`.XXXXXX`）被 Remotion 当扩展名拒绝——一分钟片的 lessons 写过，模板没改；这次改了。**写进 lessons 的模板 bug 要当场改模板。**

## 终检与收尾（第四片补充）
- **终检"漏闪"要看帧再定**：终检员用固定框量 SC39「FlashMLA」名字亮度得到单调曲线，判为漏闪；实际卡片在上滑，名字在 7027 / 7029 两帧确实全灭——固定框量运动中的元素会把闪烁抹平。主会话渲 4 张 still 就否掉了这条，比派修复省得多。**运动中的元素做闪烁检测要跟随 bbox。**
- **HUD 换词不能只靠 SoftIn**：单个条目切换时旧词硬切 + 新胶囊从 25% 重来 = 1 帧空白。模板 `Hud` 现在同章换词胶囊常驻、宽度 10 帧过渡、旧词 6 帧淡出、新词 SoftIn；章首（章节卡后）仍走 SoftIn。
- **组级 motion_check 过了 ≠ 成片过**（见上），所以流程里成片复测必须做；这次 v1→v2 因此多了一轮修复，但 v2 复验 40/40 已修、v3 只剩 4 条低项小修，两轮 QC 收口。
- 收尾顺序：v2 复验 + 终检并行（3 个 agent）→ 主会话自己做 v3 小修（都是一行改动，不值得再派）→ 渲 v3 → 主会话只对改动帧做数值抽检（HUD 行均值 / 底边行号 / 帧亮度）而不再派 QC。

---
# 第五片《CLAUDE 被偷师了》（2026-09-14，中文，58 镜头 / 3′24″，9 组，两轮 QC 后 高0/中0/低4）新增教训

## 磁盘满会静默丢 bundle 的 public/（本片最隐蔽的坑）
- 磁盘 98% 满时 `remotion bundle` **不报错但静默跳过 public/ 拷贝**：某组 bundle 0 个 ttf/0 个 wav，其余组 4 ttf+1 wav。字体加载走 `Fonts` 组件的 `.catch(() => continueRender)`，于是整组 still **静默回退系统字体**（CJK 字宽漂移、布局微移），不 hang 不报错，QC 阶段才以「字形怪」显形。**判据：`find build_dev_<tag> -name '*.ttf' | wc -l` 必须 = 4**；渲染前磁盘 ≥10Gi。已写进 QC 清单与全渲染前置检查。

## 字体与 CJK（延续第四片「共用图元字号也要过 22px」的教训）
- **FONT_MONO（SF Mono/Menlo/Consolas）没有任何 CJK 回退**，中文经它渲染走系统兜底字体——同一组件在两台机器/两个渲染期可能字形不同。**中文文案一律 FONT_HEAVY（Noto Sans SC 网络字体，确定性）**；MONO 只给代码/等宽数字。本片 SC24↔SC28 聊天气泡两套实现不一致（MonoText 22 vs FONT_HEAVY 24）就是这条的具象，统一方向选 FONT_HEAVY。
- 「共用组件同参数」判据要在**派单前写死参数值**（同字体/字号/边框/底色/节奏），否则两组各建一套（本片 SC24↔SC28 聊天窗、SC38↔SC42 手套都各写了一份，QC 阶段才发现）。

## GlitchIn rgbSplit 被不透明底盖没
- rgbSplit 的品红/青 tint 副本画在**下层**：children 若是不透明色底（TagBlock 紫 #6630F8），副本经 feColorMatrix 映射成暗品红后被上层主拷贝完全盖没，声明了等于没做（白字+裸底才可见——SC36/SC39 可见正因此）。色差感的需求要么把白字提到色底外层，要么接受删声明。

## 相机段的内容判界：世界坐标，不是屏幕坐标
- 内容安全区 x60–1220 是**世界坐标**设计界；相机推近时（screen=(world−cam)·s+640/360）任何元素在屏幕上的投影位置都会大幅移动——按屏幕坐标判「越界」会否定一切运镜（本片 SC55 ≈ 在 1.15 相机段屏幕 x25 但世界 x105，合规）。QC 报「越界」先换算世界坐标再裁定。字幕带 y637–690 除外，它是屏幕绝对域。

## QC 撤修与误报（修复波经验）
- **修复 agent 有权纠正 QC 的根因误判**：QC 称「渗漏 α 0.35 太弱」，代码实值 0.64（探针带 y560–620 实际量到的是保险箱白底边）；SC52 水环「越界 x15–55」被双检测器实证为误报（环长轴 716px 在界内）。**修复 agent 修前先复现 QC 读数；复现不了就写证据进 BUILD_NOTES 关闭，不要硬修。**
- QC 报告会在派单后继续 append（C2 从 17 行涨到 52 行）——修复 agent 派单时给的问题清单可能不全，**中途盯到新条目要补发**（本片 3 条低项因此二次唤醒才修上）。
- 卸载共用图元时连引用一起清（G6 删 GloveIcon 后 SC38 改 import G7 Glove）：跨组 import 是统一的正道（G9 SC56/58 已有先例），比单侧改形状可靠。

## 承接链重建的验证口径（SC19→SC20 案例）
- 「不离场承接」修复不能只对齐单帧：正确做法是**逐要素对比强差像素**（师傅/徒弟/线/Check/Pill/挂历/横幅 逐项 vs 未改动基线帧），并检查接管点（f2271 换组件渲染）质心是否全等——本片手搓嵌套 scale 链与组件本体有 ~1px 量化差，t=1 处必须切回组件本体才能归零。
- 相机首键跨组衔接：两个组的 CameraRig 公式若有 ±0.2px 差（(s−1)(cy−360) 项），首键要按对方 rig 等价换算，不能直接抄数。

## 多 agent（本片）
- 修复波 4 agent（各 1–2 组）并行全程无文件冲突；**跨组一致性小修（如 B1 聊天气泡统一）主会话自己做**（跨两个组的领地，派单反而要拆两份）。
- QC agent 用「全片 6105 帧逐帧标量 + 分桶通读」替代「每 40 帧目读」更密更稳，183 个尖峰逐一归位节拍/白名单——下片可作为复验回归扫描的默认方法。

---
# 主题化改造（双主题配色，2026-09-15）新增教训

## QC 判色判据跟着主题分叉，不能共用一套阈值
- frame_metrics.py 加 `--style auto` 后，三处判据都按主题分支：主体检测 dark=亮像素（lum>120）/ mint=暗像素（lum<200）；柔光 dark=中低亮高饱和 / mint=低饱和浅色带（sat>.06 且 195<lum<244）；碎片 dark=b>r>g 紫判据 / mint=accent 色相窗 ±30°。**mint 别套黑底阈值**：浅底上"柔光"是"变彩不变暗"，用亮度判据会把白卡全算进去。
- 校准教训：对**稀疏过渡帧**（镜头间空档）读数为 0 属正常，别当成检测失灵去调阈值——先用内容帧确认检测方向对了再动阈值。

## 主题化改造的回归验证方法（暗色零回归）
- 改造前先渲 3 张 Overlay still 基线（`still.sh Overlay 40,120,260`），改造后同帧重渲逐字节 MD5 对比。这次真抓到一个回归：StarField 星光 boxShadow 从 `rgba(255,255,255,.8)` 改成 `${color}`（= #ffffff 不透明），暗色下星点亮了一档——特征是**全帧稀疏像素单调变亮、无变暗**（0.4% 像素 +5 灰阶），逐像素 diff 的方向性能直接定位到"发光层 alpha 变了"。
- 改造前给关键层留"逐字节等价"的判据：dark 分支的每个值都应是改造前的原值（含 alpha、textShadow、渐变 stops），写 theme.ts 时逐项照抄，不要顺手"优化"。
- 验证环境：`cp -Rc`（APFS clonefile）秒级克隆 template 到 /tmp，`git archive HEAD -- template/src | tar -x` 覆盖回 HEAD 代码渲基线，比 stash 干净（不动工作区）。

---
# 第六片《苹果为什么敢让 Siri 迟到两年》（2026-09-16，中文，33 镜 / 2′47″，5 组，mint 主题，会话中断后接管收尾）新增教训

## 后台任务与 fin_frames 的竞态
- **渲染新版本会覆写 fin_frames/，任何还在读它的逐帧扫描任务必须先停**：本片 frame_metrics v2 扫描跑到一半时 v3 渲染已启动（bundle ~15 分钟后 chrome 才开始写帧），不停就会产出混合两版帧的假基线。规矩：`VER=vN render.sh` 前先停掉所有 `--frames fin_frames` 的后台任务，渲染完成后再启动扫描。
- 管道 `| tail -N` 会缓冲到进程结束才输出，中途看不到进度——要留档就用 `tee qc/xxx.md`，要看进度就先 `ps aux | grep frame_metrics` 确认 CPU 在跑、别急着判死。
- 渲染分两段：remotion bundle ~15 分钟（此期间 fin_frames mtime 不更新是正常的），随后 chrome-headless 并发写帧——用 `ps` 看 GPU/chrome 进程判断是否健在，不要只看帧 mtime 判"卡死"。

## QC 报告必须落盘（本片最痛的教训）
- v1/v2 的 QC 报告只存在于 agent 回报消息里，会话压缩后全丢，交付说明的质检结论只能靠 BUILD_NOTES + 可复跑命令重建。**所有 QC/motion_check/frame_metrics 报告一律 `--out` 或 tee 落盘到 `qc/*.md`**——交付说明里给"可复跑命令"不够，报告本体就是交付物。

## 组级 motion_check 偏松的具象案例与修法（本片 5 镜）
- 组级全达标、成片复测超标 5 镜：SC13 45%/1.3s（1954–1993 空窗）、SC15 36%/1.1s、SC20 41%、SC21 44%、SC29 24%/1.1s。修法两类：
  - **空窗超标首选「补贯穿持续动作」而不是挪节奏**：SC13 空窗加数据光点沿箭头流动（3 点 stagger 循环 21f），45%→14%；定稿后帧号不能动，改镜头边界不可行。
  - **加大既有持续动作的振幅/相位/速度**：呼吸光 +28% 振幅加相位、运镜加中间键（s 1.035/1.06）、行 float 2→3.5px、流光点加密。修后 v3 复测 0–34% 全达标。
- **修后验证用 PIL 探针，别直接渲全片**：目标区域 `ImageChops.difference` 数变化像素（本片实测：SC13 光点 28→143→93 accent px、SC15 芯片 3123px、SC21 调度线 425px、SC29 屏内 944px、SC20 同窗 10582 vs v2 8918）。探针秒级，全片渲染一轮 ~40 分钟。

## 边界与 hold 的数值抽查口径
- 组界/章节卡抽查：PIL diff @320×180、阈值 >12 数像素——~1900px=硬切入场正常，~160px=背景连续只换内容，~1800px 章节卡出入场，0px=设计内 hold。
- **片头/钩子段是 overlay 域，motion_check 不覆盖**：标题卡在钩子解说期间的 hold 属设计内，目检两帧确认层完整（非空帧/缺层）即可收，不用加动作。

## 主题化遗留（mint 接手暗色遗产）
- 暗色时代的白描边装饰在 mint 下失效两处：SC33 HaloRing stroke WHITE（绿渐变填充仍可读）、SC01 GhostText WebkitTextStroke 2px WHITE（浅底白描边≈无）。**主题化改造后要 grep 一遍硬编码 `#fff`/WHITE 描边**；根治是给公共组件加主题色参——本次 ui.tsx 未提供，按现状交付并写入保留项。

## 接管与收尾
- 并行会话中断后接管：先读 memory handoff 文件，再 `grep -n '遗留\|保留\|TODO' src/shots/*/BUILD_NOTES.md` 盘遗留项，逐条裁定（修复/按现状交付/写保留项），不要重查已修项。
- 收尾顺序：成片 motion_check（终检判据）→ 边界差分抽查 → 遗留项定夺 → 交付说明（占位段落渲染完立即补实测数）→ lessons 回写 → memory 更新。

## 定稿后加片头口播：T 句机制与已建组的偏移技巧
- Favor 反馈「开头标题也要有口播」后落的机制：`narration.txt` 第一个 `# CHAPTER` 前的句子 = 片头口播，tts_build 给独立 id `T01`（不占 S 编号，分镜令牌与已写分镜不用重排），压在片头卡上，片头自动延长；Overlay TITLE_RANGE 取第一章第一句（chapter>0）。
- **已建组不必改组件内任何常量**：只把 index.ts 的 from/to 改成新分镜值，组件内 F0 与节拍常量保持旧时间轴值——Sequence from 用新帧号时组件内 N = 绝对帧 − (新 from − 旧 F0)，节拍与字幕块天然对齐。句级有 ±1 帧取整漂移（浮点累加顺序变了，round 落点差 1，句时长可变 ±1 帧），在入场窗口 −6…+3 内不可见；**不要假设整片均匀平移**，index.ts 逐镜头按新分镜表填。
- 跨镜头承接的 glitch 词（上一镜头 GlitchIn、本镜头延续显示）用 GlitchText 重放同一时间函数会在 selfcheck 里数成第二次闪烁——n 已过 glitch 期的改静态渲染，视觉零变化且判据干净。

---
# 第七片《OpenAI 自曝 6 起模型失调事件》（2026-09-17，中文，50 镜头 / 4′12″，9 组，mint，两轮 QC 后 高0/中0/新增低0）新增教训

## GlitchIn 的坐标系是「最近定位父盒」（本片唯二「高」之一）
- SC41 高光词「另一版」**一帧都没渲染**：GlitchIn 嵌在墨卡 div（position:absolute）里，容器 `inset:0, overflow:hidden` 取卡为坐标系，卡内 CText 却按全局坐标 (640,415) 写 → 换算成页面 (1060,745) 画外被裁。构建组 still 自检没盖到这个词所在帧区。**规则：GlitchIn 挂在哪一层，children 必须用哪一层的坐标**——嵌套时要么 children 全改卡内局部坐标，要么把 GlitchIn 提到 CameraRig 直下用世界坐标。QC 靠「白名单词区域 ink=0」抓到（96px 大字 6000 帧零墨迹）。
- selfcheck 的白名单对账只数 **GlitchIn 出现次数**，自定义 BLINK/opacity 数组对它不可见：SC18「403」8 帧红闪用 BLINK 数组实现 → 白名单补录后反而多报一条机制不匹配。**白名单治理的前提：所有闪烁词一律标准 GlitchIn 实现**（已把 SC18 改回 GlitchIn，判据不变）。§3 清单要与分镜行同步——行内有、§3 漏登（SC14「?」/SC15「是的」）会让 problems 越补越多。

## 共用图元改几何 = 全消费方回归（本片唯二「高」之二）
- G2 构建中途把 AgentIcon 改成局部坐标画法，自己镜头跟着改了，但按**旧几何公式**摆 cy 的 SC07/SC10/SC12 全部失准——SC10 agent 头+天线直接盖住 HUD 文字。AgentIcon（2026-09-17 版）几何公式：div顶=cy−0.55s、头心=cy−0.27s、头环=cy−0.48s..cy−0.06s、天线球顶=cy−0.58s−2.5、身=cy+0.06s..cy+0.36s、label 中心≈cy+104。
- **判据进 QC：HUD 带 (616-664, 79-112) 墨水基线 ≈706，显著高于基线即有元素压 HUD**。构建组的 still 自检看不到 HUD（overlay 不在组预览合成里），这条只有 QC/复验能抓。

## 图片是本环境的上下文炸弹：QC 全数值化
- 本环境 Read 图片返回 base64 灌进上下文（曾有构建 agent 因此上下文爆掉）。本片所有 QC/修复/复验派单 prompt 带**禁读图片/HTML**条款，并落盘 `qc/QC_PROTOCOL_ENV.md` 数值协议：墨水探针（256×144 缩略图，gray<220 计数）、节拍探针（f_n−6 vs f_n+8 |Δ|≥40）、尖峰扫描逐一归因、RGB/色相扫描、int32 亮度求和。两个复验 agent 用它跑出 17/17 运镜像素级闭环（4 条 ≤2px）——**数值协议不弱于目读，且报告天然可复核**。其他环境可照常看图，派单前先探明本环境对图片的处理方式。
- 浅色主题的尖峰扫描不能用平均灰度差：128×72 均值差>8 扫出 **0 个尖峰**（小元素被均值稀释）；改成墨水像素计数（256×144，gray<220，|Δ|>120）后 1075 个。**回归扫描用计数法，不用均值法。**（第十片再证：全帧均值差>30 阈值下 mint 底结构性不可达——v2 全片实测 max 6.95，复验只能改墨水扫描（197 尖峰全归因）+ mp4 变化图 + contact sheet 三重兜底；mint 判色阈值本身也可能失效——v3 终检区域均值/p5 量化同样失灵，最后靠 19 镜 bbox 胶片目视定案。**浅色片的量化协议要在 QC 派单前先自测判据可达性，不可达就换探针类型，别让 agent 空转。**）

## 节拍探针的盲区：等量墨水换位
- SC27 高亮框换行 = 墨水挪位、总量近似不变，净差探针**结构性不可达 ≥40**（未高亮帖压暗到 α0.45 后仍 −30）；成片节拍探针实测 −179 达标，区域探针证实变化真实存在。SC22（叉描画 +112）同型。**裁定口径：净差不达标先做区域探针证「变化存在」，存在即放行并记探针盲区，不要为凑数字硬改画面。**

## 模板 API 契约（G9 实测后已补进模板组件注释）
- `CameraRig` 必须以 children 包裹内容（self-closing 是类型错误）；`TechText` 用 `text` prop 不是 children；`CText` 的 `opacity` 是 number（`.toFixed()` 字符串会静默失效）；`NoteIcon` 的 `glow` 是 string（boxShadow 值）不是布尔、w<80 胶带溢出；`THEME` 从 theme 导入（common 不导出）。**同签名不同语义的契约要在组件定义处写一行用法**，否则各构建组用「tsc 之外的猜法」各踩一遍。

## 派单工程（本片）
- **多 agent 派单消息里的 CJK 引号会打爆 Agent 工具的 JSON 解析**（一次派 2 个 QC 全失败）：prompt 落盘成 `qc/prompt_Cn.md`，派单消息只写短 ASCII 指引 + 文件路径。Write 写 CJK 文件可靠；Edit 改 CJK 不可靠，用 python 替换。
- 复验 agent 卡死（自己的脚本 NameError，600s 零产出）：SendMessage 附「先修掉 bug、边查边 append、缩略图用 256×144」唤醒后跑完。先看最后一行输出，区分「死了」和「在跑但慢」。

## 进度条分隔线：按章数等分 ≠ 按章时长等分（第七片成片反馈）
- 旧版 ProgressBar 的分隔线与章名中心都按**章数等宽**落位（(i+1)·1280/NCH），而填充右缘按**实际帧比例**走（1280·N/TOTAL）——章时长不均时（本片五章 15%/18%/23%/19%/25%）每根线与真实章界错开 52–89px，填充要晚 10–17 s 才越过线，用户一眼看出「分割线没对准」。四片历史里没暴露是因为各章时长恰好接近等分。
- 修法：分隔线 x = 该章切换帧的填充比例位置（1280·cardStart/TOTAL，cardStart=上一章末句 to+3，与高亮切换同帧——切换瞬间填充右缘恰好触线）；章名中心取相邻分隔线中点；章名槽宽按真实槽宽逐章算（窄章的槽 <1280/NCH，等宽槽会让章名压线）。已同步修进 template/src/common/ProgressBar.tsx。
- 判据：渲一章切换帧（cardStart）still，PIL 找分隔线白柱中心与填充右缘，两者差应 ≤2px；章名 ±16px 内墨水应为 0。

---
# 第八片《Jev 模型：史上最快的 AI，拒绝写句子》（2026-09-20/21，中文，47 镜头 / 4′00″，8 组，mint，两轮 QC + 终检 PASS）新增教训

## T 句修改的帧号平移与「组件 N 空间」挂载规则（本片最重要的机制认知）
- 定稿后 Favor 要求片头口播读进「Jev」→ 改 T 句重跑 tts_build，全片帧号统一 +25（S01 169→194、TOTAL 7193→7218）。**此时各组组件内部是旧时间轴的绝对帧号常量（F0=161、T_CITY=169…），且 G1 已建成**。处理：只改 G1 index.ts 的 ShotDef from/to（+25），组件内常量一律不动——`N = useCurrentFrame() + F0旧` 在新 Sequence from 下自动补偿（N旧 = 渲染帧 − Δ），动画与音频相对关系不变。第六片教训的机制正式化。
- **终检发现挂载规则的全貌**：Main.tsx 实际 `from={s.from-1}` 挂载 ⇒ G2–G8（组件 F0=from）渲染帧=源常量；**G1（本片手动 +25 过）渲染帧=源常量+25**。终检按「源常量」探 SC07 白名单词会误报漏闪——**闪烁/节拍探针必须先核每组「源常量 → 渲染帧」的映射**（方法：拿一个已知尖峰帧反推偏移）。下片建议：index.ts 生成时直接写注释标明挂载偏移。
- 通则：**定稿后改词不一定毁灭性**——T 句（片头口播）是唯一「改词不动 S 编号」的口子，代价是全片平移 Δ；只要组件内是相对时序或做一次 index 平移，就是 1 个组的成本。

## 派单组号 vs 目录分组的错位
- 修复单 A 写「G2（SC08–SC16）」但 SC13–SC16 实际在 G3 目录（分组=每章两组，不是每目录一章）→ 修复 agent 按「只改 G1/G2」正确地拒修了 3 处 G3 低项，多一轮补派。**派单帧区间要按构建组的实际镜头表写**（分镜表每组一表，照抄组号），不要按章号推断。

## selfcheck 白名单对账的盲区：parts.tsx 与手动 GLITCH_SEQ
- selfcheck 只扫 `SC*.tsx` 的 `<GlitchIn>` 调用，漏两类合规实现：① GlitchIn 封装在组内 `parts.tsx` 组件内部（G4 ExamPaper 的 titleGlitchAt prop 链路）；② 用 `common.GLITCH_SEQ` 手动重闪已入场元素（G2 SC12 的「444.6×」重闪强调，tpl=12/12 与模板逐帧吻合）。两者都是标准 12 帧模板、机制合规。修法：selfcheck 的白名单对账加「parts.tsx 内 GlitchIn 计数」+ 对 GLITCH_SEQ 手动用法按「注释声称+帧曲线核验」放行，或在分镜表白名单旁标注实现机制。

## 片尾时序：endingFade × 镜头卸载帧的跳变（第三片教训的 mint 版）
- 第三片判过「(S_last.to − endingFade) − 末字幕块起始 ≥ 30 帧」；本片踩的是它的**卸载帧变体**：压黑层是渐变时，若 `fadeIn` 在镜头卸载帧（to+1）还没压满，内容会「可读状态下单帧消失」（实测大字 min-gray 143 → 244、全帧墨水 3054→797 的 −2257 尖峰）。修法不是加大 endingFade（会重新吃满态），而是**让卸载帧恰好压满**：start = LAST_TO − ef + DELAY 中解出 DELAY，使 `to+1 − start ≥ ef`。本片最终：endingFade=6、DELAY=3（压黑 7115 起、7121 压满，大字满态 7105–7114 完整 10 帧 + 5 帧渐隐）。**判据补充：渲 to−1、to、to+1 三张 still，大字区墨水应为单调渐变、to+1 归零无 −1500 级尖峰。**
- mint 的压底色是 #F4FAF6（浅色「压浅」非「压黑」）——数值验证别用「均值下降」判 veil 生效，用「暗像素墨迹被冲淡 + 尖峰消失」判。

## QC 修复波的组织（本片顺利收口的工程经验）
- 4 个修复 agent 并行（A: G1+G2 / B: G4+G5 / C: G6+G7 / D: G8）全程无文件冲突；每单先「复现 QC 读数 → 改 → still+PIL 探针复核 → BUILD_NOTES 追加修复小节（读数前后）」。复验员发现两处 agent 互相转办的 1 行级小项（派错组的 21px 字号），主会话直接补修不回炉。
- 修复 agent 纠正了主会话修复单的两处错误（lessons 第五片授权的重演）：① 「组件 N=渲染−25」的换算对 G4–G8 不成立（实测 1:1），agent 按源码常量直接修并对证；② QC 建议的光点流叠加参数会把远层压到近不可见，agent 未逐字采纳并记录理由——都放行。
- 终检一次过：34/34 闪烁、8/8 高光、25/25 运镜、motion 47/47。终检 agent 中途覆盖面没跑完（23 处运镜），SendMessage 附「以分镜表实列数为准 + 扩展现有 probe 脚本 + 每章抽 1 处查 HUD」一次续跑收口。
- 尖峰扫描必须逐帧 step=1：本片先按隔帧采样（step=2）把 1033 个尖峰扫成 0，逐帧重跑才全部归因；成片帧文件名是 f_%04d 四位数（脚本按 %05d 建名找不到文件）。

## 主会话等待模式：回合内长 sleep 是反模式（用户三次问「目前进度如何」全部插在 sleep 里）
- 根因三连：① 依赖链全程串行，每步之间主会话无事可做，把「等」做成回合内 sleep；② 「长 sleep 比频繁轮询省 token」的错误算盘——整段不可响应，agent 提前完成也得睡到点才醒（完成通知在工具调用间隙才送达，sleep 期间排队）；③ 架空了 harness 通知机制——后台 agent 完成自动唤醒、后台 Bash 退出有退出通知，本就是为了免轮询。连带伤害：卡死检出变慢，sleep 里「卡死」与「在跑但慢」不可分辨（调研 agent 卡 20 分钟才发现）。
- 规则：① 派单后主会话无事可做 → 结束回合（留言「完成会自动继续」），等 task notification 唤醒；② 必须盯的任务 → 短 sleep ≤120s + 磁盘探针（文件大小/mtime 增长、ps 查进程），连续 2–3 个周期零变化判卡死，SendMessage 唤醒或 TaskStop 释放；③ 渲染等长任务一律 run_in_background，用退出通知接续；④ 被问进度 → 立即查盘汇报，不续睡。

---
# 第九片《史上最快的 AI，不会数数》（2026-09-22，中文，38 镜头 / 4′20″，8 组，mint，v2 全章 QC + 修复波 + v3 终检 PASS）新增教训

## 承接直通模式正式化：同元素跨组界 α=1 连续
- 第三次踩「板/卡跨组界」后沉淀为通则：**同一元素两个镜头共用同组件同参数时，前镜该元素移出 exitOp 包裹层、后镜首帧 opacity=1，元素 α=1 跨组界直通**（胶囊/标签等新入场物留在 ex 层照常离场）。先例链：SC24→SC25 探头、SC33→SC34 算盘、本片 SC25→SC26 两栏板（修复前板随前镜 exitOp 归零又以 firstOp 57% 重入 = 组界眨帧）。frame_metrics 的组界 diff 探针：<300px 视为连续（JPEG 噪声量级），数千 px 即跳变。

## 修复单要给「契约矩形」而不是只给现象（QC 中项一次修对的根因）
- SC15→SC16 组界跳变（281×352 vs 259×324）的根因是 wrapper scale 插值两端皆错（1→1.0833，应为 0.923077→1）——**settle 态被放大 8% 且首帧非探头态，一错双端**。QC 报告若只写「SC15 末帧对齐契约矩形」会诱导只修一端；写明「契约 = SC16 首帧 260×325 @ (510,190) + 探头态 240×300 @ (1040,210) rot6」后，一个插值修复同时治好两端组界。修复单/裁定记录要带矩形四元组（x,y,w,h）与旋转。
- 验证手段：边界两帧 PIL 像素 diff（>30 阈值计 px 数），与本镜头内 2 帧步进的 diff 对比——组界 diff **低于镜头内自然步进**才是真连续。

## QC 报告的「其余 N 处均为 X 样式」必须抽查后才能当修复依据
- C2 报告称「其余 6 处 GlitchIn 词均为 mint 胶囊（CText）」，实际盘点：4 处描边胶囊 + 1 实心胶囊 + 1 blob 轮廓——结论对但描述不准（不同三种容器）。若照报告字面「统一为 CText」会修错方向。**裁定前逐处 grep 渲染体核实容器类型**，修复以「多数派几何」为统一目标（本片：Box transparent + sw2 + CText 24/900），语义色（WARN/ACCENT）保留不动。

## frame_metrics 低标记的裁定经验表（两片累积，直接引用免重判）
- 重点色碎片 ≥8：图标/胶囊描边/芯片 pin 阵列等规则图形 = 判据 §6 已知误报，帧检放行（本片 SC02/05/06/08/09/10/16/19/21/22/26/28/29/30/31/34/36/38 全部此类）。
- 主角无光：光在 overlay 层（扫光/章卡）、光在灰配角区、或 mint 判色对白底偏色 = 豁免；逐镜头过一遍 still 确认光实际在即可。
- 主体 <170px：先算实际高度（300×300 方卡实际 300px 达标）再信折算中位——折算高度 min(w,4h)/2.5 对方形/横版物体系统性偏低。
- 主角区柔光中位数会被建组期拉低（第十片 SC30）：148f 横向时间线渐进建组期最大物体是非主角的章/HUD（读数 17–706px²），卡体真正在场期实测 ≈39,000px²（全片主角级中位档）——**「中位数」口径对「前半建组 + 后半主角」的镜头必然偏低，逐帧复算在场窗口才算数**；分镜光列明确要求的光已存在即「分镜原样」不动。
- motion_check 对 <30px 小元素不可见：小面积动作（光点沿虚线跑、8px 点滚动）在组级/成片读数里都不计入，最长静止可能全靠主角撑——判「真静」前先看画面里有没有小元素动词在跑，别只信读数。

## 重复派单事故：同 brief 派两个 agent = 资源互踩（本片唯一事故）
- 同一修复 brief 派出两个 agent（ListAgents 查重被跳过）：两者独立得出一致修法（互为交叉验证）但互踩资源——同名 bundle tag、渲染清理互相中止、/tmp 工作目录互删、BUILD_NOTES 写出重复段。处置：去重合并 + 事故追记。规则：**派单前 ListAgents 查重；并行 agent 各配私有 tag 与私有 /tmp 目录；发现重复立即 TaskStop 一个**。
- 关联：render.sh 的 bundle 清理护栏在并行 agent 场景下会误伤——修复 agent 的测渲与主会话渲染不能同时跑（排队或错峰）。

## still.sh 是 zsh 脚本（第二次踩）
- `bash scripts/still.sh` 报 `${(s:,:)FRAMES}: bad substitution`。still.sh 用 zsh 数组切分语法，**一律 `zsh scripts/still.sh` 调用**。已写入多次仍会顺手敲 bash——修复 agent 的派单 prompt 里显式写「用 zsh 调 still.sh」。

## countTo 返回 fmtInt 字符串（带千分位）——年份/编号类数字要局部去逗号
- `countTo` 返回 `fmtInt`（千分位逗号）供 2,000/9,999/29,000 类计数；年份 1865、题号等按分镜表不带逗号，须 `.replace(/,/g,'')` 局部处理。分镜表上「1865」样式的数字在排镜头时就要标注，QC 探针帧才能对上。

## tts_build 零/负时长字幕块 → 1 帧双行叠印（第十片实锤）
- 对齐偶发给某词 0 长度 → 块被钳成 from==to，随后重叠修正又压成 to=from−1 → 渲成 **1 帧双行 44px 叠印乱字**（f_2376「Maven」叠在上一块上，QC 报「中」）。修法（template + 项目副本双改）：修正字幕循环里 `if sb['to'] <= sb['from']: continue` 直接丢弃零/负时长块，全量收尾后 `assert not [sb for sb in all_subs if sb['to'] < sb['from']]`（对齐故障宁可报错重跑，不要静默出鬼帧）。

## GlitchIn 容器必须给定宽高（内层恒 absolute inset:0）
- GlitchIn 内层是 `absolute inset:0` 的 sliced 包装——容器若用 `relative + inset:auto`（随内容收缩）会**塌成 0×0**，白名单词错位/不可见（f_1510 首版实证）。容器必须给定宽高盒（SC06 案例：left 294 / 392×96，整行盒 = 各段宽和，行内多段同基线）。修 glitch 断词时先算好盒宽再包。

## 提字号必须连固定宽度容器一起算
- 22px 下限修复不是改 fontSize 一行：**固定宽度容器会被顶爆**。实锤两个：SC12 日历日期 20→22 时 Orbitron 10 字符 ≈168px 顶爆 160px 卡宽 → CAL.w 160→178（左缘固定向右长，环钉/日期自适应 CAL.w）；KillListCard AUTO 17→22 时 70px 胶囊装不下 → 78 加宽（右缘不变法：left −8、width +8）。修完必出 still 验「文字在盒内」。宽字体（Orbitron/Audiowide）按 ~0.9em/字符估宽。

## 共享组件先 grep 使用方再改；被遮挡元素两轮 QC 都会漏标
- KillListCard 被 SC13/SC18/SC36 三个组使用（G4 经 shared.tsx 迁 G3 正式件）：修 AUTO 字号只看 SC13 会漏 SC18（0.75 等比缩放用卡，内部比例不变才安全）与 SC36（无章遮挡全可见）。**改任何共享组件前 grep 全部 import 方，逐一出 still。**
- 同理：QC 两轮都没标 KillListCard 的 17px AUTO——SC13 里被「替你选好」章压住。**被前景元素遮挡的 QC 漏标区，修同族项时要主动排查同组件其他使用方**，否则 SC12 修了 SC13/SC36 没修，终检通读逮到不一致。

## chrome-headless-shell 下载卡死 → 从兄弟项目拷
- 新项目 npm install 后 remotion 首次渲染要下 chrome-headless-shell，本环境偶发下载挂起（进度 0 卡 10 分钟+）。修法：从已建成的兄弟片项目拷 `node_modules/.cache/remotion`（或其 chrome-headless-shell 目录）过来即用，不走下载。

## 22px 下限的 QC 口径：辅助小字族 vs 主内容文字
- 质量标尺「文字 ≥22px」在两轮 QC 实操里判的是**显著文字**：SC10/12/13/14 的章内标注/标签/灰行被标（中/低），而各镜 18px「示意」角标、20px 徽章数字、16px 步骤号、15px 等宽小注两轮全未标——辅助小字（角注/徽章/刻度）是容忍族。主会话裁定口径要写进 BUILD_NOTES 让终检照案归档（本片记在 G3「QC v1 处置」#5）；新片若想全片严格 22px，在分镜表全局约束里写明，别靠 QC 事后判。

## 【meta-muse 第十一片】mint 主题下 GREY_LINE/浅灰描边对工具与人眼双重隐形
- 分镜要求「消息板容器 2594 入场」，实现用了 GREY_LINE #C7D2CB（lum≈206）画线——frame_metrics 主角判据 lum<200 读不到 → 空场 108f 高项；人眼在浅幕底上也几乎看不见 → v1 真缺陷。修法：实线墨线（THEME.fillDim 底 + LINE 2.5px 描边）。**mint 主题下任何「淡」「灰」元素上线前先算 lum，<200 才算主角可见。**

## 【meta-muse】派修会引入次生缺陷：入场轨迹实线化后「显形」越字幕带
- SC14 容器派修把隐形灰线换成实线墨线后，原本就存在的 slideUp Δ80 轨迹（首帧底缘 698）在 v2 显形横穿字幕带 10 帧——v1 因隐形没被 QC 抓到。**修「可见性」类缺陷时必须重查该元素的入场/离场轨迹**（越 y637、穿字幕带、出安全区），不能只验「看得见了」。G8 先例同因（底沿 657 → Δ90 改 60）。

## 【meta-muse】motion_check 两类新伪报口径（mint 主题，QC 判读照此）
- **浅底滑动稀释**：fillDim(#E7EFEA) vs 幕底(#F4FAF6) 灰差 ≈11 级，320×180 采样下浅板 slideUp 灰变稀释至阈值下 → 「真静」伪报（实测在动）。**run 混样稀释**：一个 run 横跨「真静止 + 小面积动作」时中位数被静止帧稀释 → 752px 伪报（游标实跑）。
- **320×180 小事件盲区**：26px CJK 打字机（全分辨率 ~380px/事件）与 SoftIn 小行在该采样下不可见，成片复测连读成 4.4s「still」；全分辨率手工核算 <3s 才是真值。判据：**分段手工核算 + 目标色像素逐帧实测为准**，工具读数豁免要写进 metrics_rulings 供终检沿用。
- 打字机要「断开 run」就得让单字事件 >800px：小字号打字机配合加粗块光标（13×30→16×34，移位 ≈990px）才有效；纯文字事件永远断不开。

## 【meta-muse】frame_metrics 不能与渲染并行：fin_frames 被清空重写
- render.sh 先清空 fin_frames/ 再逐帧写——并行跑 frame_metrics.py 会在读到一半时 FileNotFoundError（frame_7438 事故）。量化存档要么渲前跑，要么等渲完；**任何读 fin_frames 的脚本/agent 都不与渲染同时跑**。

## 【meta-muse】montage 文件清单必须 @filelist 语法
- 拼接触表把文件列表放 shell 变量再展开，带空格路径会被当成**单个文件名**报「unable to open image ' fin_frames/…'」。正确姿势：`ls | ... > /tmp/list.txt` 后 `montage @/tmp/list.txt …`。字体警告无害可忽略。

## 【meta-muse】章首镜头 softOp 全镜淡入是管线惯例不是缺陷
- `softOp(n, len)`（ui.tsx）让每镜前几帧整镜淡入：softOp(0,5)≈0.335。四个章首镜头（承接 Pill 等）首帧 ~34% 透明度被 QC 报「发白/变淡」——**全片 10 组同一约定，判「设计」不修**；复验前把该口径写进派单。

## 【meta-muse】片尾序列的代码真值：压色层/EndCredit/纯幕底三段
- ENDING_RANGE=[LAST_TO−endingFade(30), TOTAL] 压幕底色（mint 是 #F4FAF6 非黑！）、END_CREDIT_RANGE=[LAST_TO+1, TOTAL−26] 署名卡 fadeIn(0,8)（第 0 帧全透明——fin 帧看着「空白」是设计不是缺陷）、EndingTop aboveBar 压进度条。分镜「末 40 帧纯黑」是模板暗色时代措辞——**新片分镜片尾行照代码写：压幕底色层 → EndCredit → 末 26f 纯幕底**。

## 【meta-muse】派修复时把「误判报实情勿强改」写进 prompt
- C3 QC 报 SC20 角标「间隔点带空格」→ 修复 agent hexdump + 逐列像素实测证明源码本就无空格（全角「·」字形自带留白），按指令报实情未改码——避免了一次假修。**修复/复验 agent 的 prompt 末尾固定带这条逃生口**。

## 【meta-muse】终检派单里的清单要照抄分镜表，不要凭记忆转写
- 终检 prompt 手写「C2 SC15 高光」实为 SC11（SC15 是章末钩子无★）——终检 agent 靠读分镜表 §6 自行纠正并备注。高光/运镜/白名单清单**直接引用 §6/§7/白名单节原文或让 agent 读表**，转写必错。

## 【meta-muse】逐镜栏运镜帧数与章级口径的裁量
- §7 头部写「30–45 帧」，逐镜栏自定 24–28f（SC05/07/10/11/23）——QC/终检按「分镜逐镜栏为准」放行，无观感缺陷。分镜表若要全局硬约束就在 §7 写明「逐镜栏不得低于 30f」，否则两套口径并存属正常。

## 【meta-muse】FONT_MONO 宽度估算陷阱
- FONT_MONO 按 0.6em/char 直接锚定可用；对 textW() 结果再乘 0.6 系数会双重折算右偏——mono 宽度只算一次。

